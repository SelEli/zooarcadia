{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>
        </div>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ comment.name }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comment.date ? comment.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Visible</th>
                <td>{{ comment.isVisible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comments_index') }}">Retour à la liste</a>
    <a href="{{ path('app_comments_edit', {'id': comment.id}) }}">Éditer</a>

    {{ include('comments/_delete_form.html.twig') }}
{% endblock %}
