{% extends 'base.html.twig' %}

{% block title %}UsersRoles{% endblock %}

{% block body %}
    <h1>UsersRoles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ users_role.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ users_role.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_users_roles_index') }}">back to list</a>

    <a href="{{ path('app_users_roles_edit', {'id': users_role.id}) }}">edit</a>

    {{ include('users_roles/_delete_form.html.twig') }}
{% endblock %}
