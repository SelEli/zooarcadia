{% extends 'base.html.twig' %}

{% block title %}Editer un service{% endblock %}

{% block body %}
    <h1>Editer un service</h1>
        </div>
    </div>
    
    <div class="container bg-secondary p-4">
        {{ include('services/_form.html.twig', {'button_label': 'Editer'}) }}

        <a href="{{ path('app_services_index') }}">Retour à la liste</a>

        {{ include('services/_delete_form.html.twig') }}
    </div>
{% endblock %}
