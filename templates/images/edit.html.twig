{% extends 'base.html.twig' %}

{% block title %}Éditer l'Image{% endblock %}

{% block body %}
    <h1>Éditer l'Image</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('app_images_index') }}">Retour à la liste</a>

    {{ include('images/_delete_form.html.twig') }}
{% endblock %}
